[project]
name = "module4"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "django>=5.2.4",
]

[dependency-groups]
dev = [
    "ruff>=0.12.7",
]
